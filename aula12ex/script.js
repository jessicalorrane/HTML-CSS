    function verificar() {
 var data = new Date ()
 var ano = data.getFullYear ()
 var fano = document.getElementById('txtano')
 var res = document.querySelector('div#res ')
 if ( fano.ariaValueMax.length ==0 || Number(f.ano.value) > ano) {
    window.alert ('[erro] verifique os dados e tente novamengte !') {
    }   else {
        var fsex = document.getElementsByName ('radius')
        var idade = ano Number(fano.value)
        res.innerHTML = ano- Number (fano.value)
        res.innerHTML = 'idde calculada : $(idade)'

        }
    }
 }
    }
